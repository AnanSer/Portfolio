import { Fragment } from "react";
import HomePage from "../pages/homePage/HomePage";

const Wrapper = () => {
  return (
    <Fragment>
      <HomePage />
    </Fragment>
  );
};

export default Wrapper;
